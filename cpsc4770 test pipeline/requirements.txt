# HLE Gemini 2.5 Flash evaluation pipeline
# Install with: pip install -r requirements.txt

datasets>=2.18.0          # HuggingFace datasets (loads cais/hle)
openai>=1.30.0            # o3-mini judge (structured outputs / .parse())
httpx>=0.27.0             # async HTTP for Gemini REST calls
tqdm>=4.66.0              # progress bars
numpy>=1.26.0             # calibration error calculation
pydantic>=2.0.0           # ExtractedAnswer schema for judge
